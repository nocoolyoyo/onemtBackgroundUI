<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wkt.dao.jpushinfo.IJpushInfoDao">
	
	
	<!-- 新增极光推送信息 -->
	<insert id="insertJpushInfo" parameterType="java.util.List">
		INSERT INTO yj_jpush_info (
				registrationId,
				notification_title,
				msg_title,
				msg_content,
				extrasparam,
				data,
				create_time
			)
		VALUES
			<foreach collection="list" item="item" index="index" separator="," >
			(
				#{item.registrationId},
				#{item.notification_title},
				#{item.msg_title},
				#{item.msg_content},
				#{item.extrasparam},
				#{item.data},
				#{item.create_time}
			)
			</foreach>
	</insert>
	
</mapper>